import React from 'react';
import { CardPlace } from 'io-sanita-theme/components';

const ResultItem = ({ item }) => {
  return <CardPlace item={item} type="synthetic" />;
};

export default ResultItem;
